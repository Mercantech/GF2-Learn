---
title: "Betingelser"
order: 3
topics: [betingelser, if]
kompetencemaal:
  - "Kan bruge if/else til at styre programflow"
timer: 2
---

# Betingelser

Programmer tager beslutninger med `if`:

```csharp
int score = 85;
if (score >= 60)
    Console.WriteLine("Bestået!");
else
    Console.WriteLine("Ikke bestået");
```

:::git-step
commit: "feat: betingelser demo"
branch: main
:::
